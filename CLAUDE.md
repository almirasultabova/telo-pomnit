# CLAUDE.md — Правила работы с проектом «Тело помнит»

## Обязательно прочитай project.md перед началом работы

---

## Структура файлов

Каждая страница — **один самодостаточный HTML-файл**.
Весь CSS и JS — внутри файла, в тегах `<style>` и `<script>`.

```
telo_pomnit/
├── landing.html      # Лендинг v1 — светлый, анимированные блобы
├── landing2.html     # Лендинг v2 — editorial, крем + зелёный
├── thanks.html       # 🔜 Страница «Спасибо» (планируется)
├── payment.html      # 🔜 Страница оплаты (планируется)
└── project.md        # Описание проекта и роадмап
```

---

## Технические ограничения

- **Никаких внешних зависимостей** — без Bootstrap, Tailwind, jQuery, Google Fonts, CDN
- **Никаких фреймворков** — чистый HTML + CSS + vanilla JS
- **Иконки** — только инлайн SVG прямо в HTML
- **Шрифты** — только системные:
  - Заголовки: `Georgia, 'Times New Roman', serif`
  - Текст: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`

---

## Дизайн-система

Проект имеет **два дизайна**. Новые страницы (`thanks.html`, `payment.html`)
делаем в стиле того лендинга, к которому они примыкают, либо спрашиваем.

### landing.html — «Живой, яркий»
| Токен | Значение |
|---|---|
| `--bg` | `#fdfaff` |
| `--primary` | `#7c3aed` (фиолетовый) |
| `--accent` | `#c026d3` (малиновый) |
| `--text` | `#1a0530` |
| Фон | 4 анимированных CSS-блоба с `filter: blur(120px)` |
| Анимация hero | SVG-силуэт тела + пульсирующие кольца |

### landing2.html — «Editorial, сдержанный»
| Токен | Значение |
|---|---|
| `--bg` | `#f7f4ef` (тёплый крем) |
| `--green` | `#2a4a38` (лесной зелёный) |
| `--gold` | `#c49a3c` |
| `--text` | `#1a110a` |
| Фон | Одно дышащее кольцо в hero, белые секции чередуются с кремом |
| Заголовки | Georgia italic |

---

## Адаптивная вёрстка

Оба брейкпойнта **обязательны** в каждом файле:

```css
@media (max-width: 768px) { /* планшет и мобильный */ }
@media (max-width: 480px) { /* маленький мобильный */ }
```

**Правила:**
- На мобильном все grid-сетки сворачиваются в 1 колонку
- Навигация: гамбургер-меню на `≤ 768px`
- Кнопки: полная ширина на `≤ 480px`
- Шрифты масштабируются через `clamp()`
- Отступы уменьшаются на мобильном (обычно вдвое)
- Тестируй вёрстку при ширине 375px (iPhone SE) и 768px (iPad)

---

## Анимации

**Используем только:**
- CSS `@keyframes` для фоновых эффектов и breathing-кружков
- `IntersectionObserver` + класс `.reveal` → `.visible` для появления элементов при скролле
- CSS `transition` для hover-эффектов

**Не используем:**
- Canvas для лёгких эффектов (только если действительно нужно)
- Тяжёлые JS-библиотеки анимаций

---

## Стиль текста (копирайтинг)

Проект работает с русскоязычной аудиторией. Тон — **тёплый, прямой, без глянца**.

- Обращение к читателю: **«вы»** (с маленькой буквы)
- Без восклицательных знаков в заголовках
- Без агрессивных продающих фраз («только сегодня», «успей»)
- Короткие предложения, много белого пространства
- Курсив (`<em>`) для смысловых акцентов, не для украшения
- Кавычки: **«ёлочки»**, не "лапки"
- Числа прописью только если это органично, цифры предпочтительнее

---

## Перед созданием нового файла

**Обязательно сначала опиши:**
1. Какой файл создаёшь и зачем
2. Какой дизайн используешь (landing.html или landing2.html)
3. Из каких секций будет состоять страница
4. Какие новые элементы появятся (если есть)

Дождись подтверждения — только потом пиши код.

---

## Перед каждым коммитом

- Проверь, что HTML валиден (нет незакрытых тегов)
- Убедись, что страница открывается локально
- Проверь мобильную вёрстку (DevTools → 375px)

---

## Git и GitHub

- Репозиторий: `https://github.com/almirasultabova/telo-pomnit`
- Ветка: `main`
- gh CLI находится в `C:\gh-cli\bin\gh.exe`
- Сообщения коммитов — на русском или английском, коротко и по делу
